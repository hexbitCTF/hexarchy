#!/bin/bash

# hexarchy:summary=Dismiss a notification by summary substring. Used by the first-run notifications to dismiss them after clicking for action.
# hexarchy:args=<summary>

if (($# == 0)); then
  echo "Usage: hexarchy-notification-dismiss <summary>"
  exit 1
fi

hexarchy-shell -q notifications dismiss "$1"
