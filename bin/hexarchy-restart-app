#!/bin/bash

# hexarchy:summary=Restart an application by killing it and relaunching via uwsm.
# hexarchy:args=<application-name> [application-args...]

pkill -x $1
setsid uwsm-app -- "$@" >/dev/null 2>&1 &
