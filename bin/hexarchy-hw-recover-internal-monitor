#!/bin/bash

# hexarchy:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/hexarchy/toggles/hypr/internal-monitor-disable.lua"

if [[ -f $TOGGLE ]] && ! hexarchy-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
