#!/bin/bash

# hexarchy:summary=Install Signal and launch it.
# hexarchy:requires-sudo=true

set -e

echo "Installing Signal..."
hexarchy-pkg-add signal-desktop

echo "Opening Signal..."
setsid uwsm-app -- /usr/bin/signal-desktop >/dev/null 2>&1 &

echo ""
echo "Signal has been installed."
