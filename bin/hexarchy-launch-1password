#!/bin/bash

# hexarchy:summary=Launch 1Password or start its installer when missing.

set -e

if hexarchy-cmd-present 1password; then
  exec setsid uwsm-app -- 1password
else
  exec hexarchy-launch-floating-terminal-with-presentation hexarchy-install-service-1password
fi
