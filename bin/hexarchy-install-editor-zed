#!/bin/bash

# hexarchy:summary=Install Zed Editor and configure it with the current Hexarchy theme

echo "Installing Zed Editor..."
hexarchy-pkg-add zed omazed

# Apply Hexarchy theme to Zed
omazed setup

setsid uwsm-app -- gtk-launch dev.zed.Zed >/dev/null 2>&1 &
